# 구현 검증 보고 · 2026-09-08

## 결과

새 구현의 자동 시험 **70건 모두 통과**했다. 제3본 예제 25건을 RP·GA 적용 범위별로 나눈 **43개 코드포인트 대조**가 포함된다. 나머지는 구조 보존, 미결 처리, 참조·소비 검사, 사전 출처와 오류 처리를 확인한다.

기존 앱의 시험은 별도로 **115건 모두 통과**했다. 구버전 자체의 규칙을 보존했다는 뜻이며, 구버전이 제3본 기준에 적합하다는 뜻은 아니다.

| 검사 | 결과 |
| --- | --- |
| 제3본 직렬화 대조 | 43 / 43 |
| 새 엔진·사전 시험 전체 | 70 / 70 |
| 기존 spec.test.mjs | 18 / 18 |
| 기존 test.mjs | 30 / 30 |
| 기존 modes.test.mjs | 52 / 52 |
| 기존 articulation.test.mjs | 15 / 15 |
| JavaScript 구문 | 통과 |
| 로컬 HTML·CSS 자산 및 문서 경로 | 통과 |
| 입력 라벨·스크립트 대상 ID | 통과 |
| 두 WOFF의 제3본 핵심 부호 수록 여부 | 누락 없음 |

## 주요 검사

- boost의 /uː/ 토큰 1개 → JV-U 요소 2개.
- cats의 /t+s/ 토큰 2개 → JV-TS 1개, 음절 경계를 가로질러 합치지 않음.
- play·blue·hello에서 /l/을 중복 소비하지 않음.
- FOOT와 U U, GA AW와 RP O O의 대립 보존.
- 미배당 종성 5개는 구조에 남기고 단어 전체 평문 출력을 중단.
- GA /ɝ/와 음절자음에 임의 모음 삽입 없음.
- GA /ɚ/의 원문과 /əɹ/ 정규화 이력 보존.
- 강세·원문·출처가 평문만으로 보존된다고 주장하지 않음.
- 잘못된 부호 순서·채움문자·참조·중복 소비 거부.
- 점 경계 우선, 자동 음절 분석의 검토 필요 상태 표시.
- 사전 요청 실패 후 재시도 가능, 없는 단어에 발음을 생성하지 않음.

## 재현

```sh
node tools/check.mjs
node --test tests/*.test.mjs
node tools/build.mjs
```

이 보고는 코드·정적 자산 검증 결과다. 실제 브라우저의 화면 배치·클립보드·다운로드·키보드 조작에 대한 종단간 시험과 GitHub Actions 실행·게시 확인은 아직 수행하지 않았다. 따라서 웹앱 전범위 정확도, 모든 브라우저의 글꼴 합성, 공개 배포 완료를 뜻하지 않는다.
